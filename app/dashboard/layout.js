export const metadata = {
  title: "Venue Dashboard",
  description: "Manage your pub's fixture listings and venue details on SportSpot.",
  robots: { index: false, follow: false },
};

export default function DashboardLayout({ children }) {
  return children;
}
