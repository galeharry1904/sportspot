export const metadata = {
  title: "Plan Your Matchday",
  description:
    "Build a day plan and get pub suggestions based on where you'll be and when kickoff is.",
};

export default function PlanLayout({ children }) {
  return children;
}
