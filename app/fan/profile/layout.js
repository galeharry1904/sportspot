export const metadata = {
  title: "My Profile",
  description: "Manage your SportSpot fan profile, favourite teams, and preferences.",
  robots: { index: false, follow: false },
};

export default function FanProfileLayout({ children }) {
  return children;
}
