export const metadata = {
  title: "Fan Sign Up",
  description:
    "Create a free SportSpot account to save your favourite teams and find pubs showing their games.",
};

export default function FanRegisterLayout({ children }) {
  return children;
}
